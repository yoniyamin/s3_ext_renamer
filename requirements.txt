# Core runtime dependencies
boto3>=1.34.0,<2.0.0
Flask>=3.0.0,<4.0.0
pywebview>=5.0.0,<6.0.0

# Development/testing dependencies
pytest>=8.0.0,<9.0.0
requests>=2.31.0,<3.0.0

# Build dependencies (only needed when creating executables)
PyInstaller>=6.0.0,<7.0.0