# Precise production requirements with exact versions
# Generated for clean builds to avoid dependency bloat

# Core application dependencies
boto3==1.38.39
Flask==3.0.3
pywebview==5.4
pystray==0.19.5
Pillow==11.3.0

# Build dependency (only for creating executables)
PyInstaller==6.14.1

# Testing (optional - remove for production-only builds)
# pytest>=8.0.0,<9.0.0
# requests>=2.31.0,<3.0.0
